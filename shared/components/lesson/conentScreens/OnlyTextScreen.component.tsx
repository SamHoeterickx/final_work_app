import { FC } from 'react';
import { Text, View } from 'react-native';

export const OnlyTextScreen: FC = () => {
    return (
            <View>
                <Text>ONLY TEXT</Text>
            </View>
        );
};